rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Public read; authenticated writes. Adjust as needed before production.
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
